# Copy to .env in this folder (same as backend/.env in the repo). Never commit .env.
#   cp .env.example .env

# Google AI Studio / Gemini — use one:
# GOOGLE_API_KEY=
# GEMINI_API_KEY=

# Optional:
# GEMINI_MODEL=gemini-2.5-flash
# DESIGN_AI_PROVIDER=gemini
